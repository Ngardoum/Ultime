<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>RestauConnect - Menus</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <%= stylesheet_link_tag 'all_menus', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>

<body>

  <!-- Barre de navigation principale avec logo et icônes sociales -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <!-- Logo du site -->
      <a class="navbar-brand" href="#">
        <%= image_tag 'logo3.png', class: 'd-block w-100', alt: 'Restaurant 1' %>
      </a>

      <!-- Icônes de réseaux sociaux -->
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav social-icons">
          <li class="nav-item">
            <a class="nav-link" href="https://facebook.com" target="_blank"><i class="fab fa-facebook"></i></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="https://tiktok.com" target="_blank"><i class="fab fa-tiktok"></i></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="mailto:example@gmail.com"><i class="fas fa-envelope"></i></a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Deuxième barre de navigation avec les liens du site -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light mt-2">
    <div class="container-fluid">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarLinks" aria-controls="navbarLinks" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarLinks">
        <ul class="navbar-nav mx-auto">
          <li class="nav-item"><a class="nav-link active" href="#">Accueil</a></li>
          <li class="nav-item"><a class="nav-link" href="<%= all_menus_path %>">Menus</a></li>
          <li class="nav-item"><a class="nav-link" href="<%= all_restaurants_path %>">Restaurants</a></li>
          <li class="nav-item"><a class="nav-link" href="#gallery">Galerie</a></li>
          <li class="nav-item"><a class="nav-link" href="#contact">Contact</a></li>
          <li class="nav-item"><a class="nav-link" href="<%= new_user_session_path %>">Connexion</a></li>
          <li class="nav-item"><a class="nav-link" href="<%= new_user_registration_path %>">Inscription</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Section d'affichage des restaurants -->
  <div class="container mt-4">
    <div class="row">
      <% @restaurants.each do |restaurant| %>
        <div class="col-md-4">
          <div class="card mb-4 shadow-sm">
            
            <!-- Affichage du logo du restaurant ou d'une image par défaut -->
            <% if restaurant.logo.attached? %>
              <%= image_tag restaurant.logo, class: 'restaurant-logo-img img-fluid', alt: restaurant.name %>
            <% else %>
              <%= image_tag 'default_image.jpeg', class: 'card-img-top', alt: restaurant.name %>
            <% end %>

            <!-- Détails du restaurant -->
            <div class="card-body">
              <h5 class="card-title"><%= restaurant.name %></h5>
              <p class="card-text"><%= restaurant.slogan %></p>
              <p class="card-text"><%= restaurant.address %></p>
              <%= link_to 'Voir Restaurant', restaurant_path(restaurant), class: "btn btn-primary" %>

              <!-- Lien pour démarrer une conversation avec le restaurant -->
              <h6 class="mt-3">Conversations :</h6>
              <ul>
                <li>
                  <%= link_to 'Démarrer une conversation', new_conversation_path(restaurant_id: restaurant.id), class: "btn btn-secondary" %>
                </li>
              </ul>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

</body>
</html>
