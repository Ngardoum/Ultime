<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RestauConnect - Menus</title>
  <!-- Importation des icônes FontAwesome et des feuilles de style -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <%= stylesheet_link_tag 'all_menus', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <!-- Barre de navigation avec logo et icônes sociales -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">
        <%= image_tag 'logo3.png', class: 'd-block w-100', alt: 'Restaurant principal' %>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav social-icons">
          <li class="nav-item">
            <a class="nav-link" href="https://facebook.com" target="_blank">
              <i class="fab fa-facebook"></i>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="https://tiktok.com" target="_blank">
              <i class="fab fa-tiktok"></i>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="mailto:example@gmail.com">
              <i class="fas fa-envelope"></i>
            </a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Section d'affichage des détails du restaurant -->
  <div class="container my-5">
    <!-- Logo du restaurant -->
    <div class="restaurant-logo mb-3 text-center">
      <% if @restaurant.logo.attached? %>
        <%= image_tag @restaurant.logo, class: 'restaurant-logo-img img-fluid', alt: @restaurant.name %>
      <% else %>
      <% end %>
    </div>

    <!-- Informations du restaurant -->
    <div class="restaurant-details text-center">
      <h2><%= @restaurant.name %></h2>
      <p><%= @restaurant.slogan %></p>
      <p><%= @restaurant.address %></p>
      <p>Type de Cuisine : <%= @restaurant.cuisine_type %></p>
      <p>Pays : <%= @restaurant.country %></p>
      <p>Latitude : <%= @restaurant.latitude %></p>
      <p>Longitude : <%= @restaurant.longitude %></p>
    </div>
  </div>

  <!-- Scripts Bootstrap -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
